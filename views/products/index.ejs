<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document Handle</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        body{
            background-color: rgb(161, 145, 145);
        }
        .container{
            
            width: 100vw;
            height: 100vw;
            margin: auto;
        }
        h1{
            text-align: center;
        }
        .main{
            display: flex;
            flex-wrap: wrap;
            justify-content: center;    
            gap: 20px;
        }
        .cardlink, .role{
            text-decoration: none;
            color: black;
        }
        .myCard, .card{
            background-color: white;
            border-radius: 10px;
            height: 200px;
        }
        .card-title{
            text-align: center;
            font-size: 30px;
        }
        .role{
            color: rgb(232, 21, 21);
        }
        .press{
            background-color: rgb(186, 45, 45);
            display: inline-block;
            font-size: 25px;
            padding: 5px;
            border-radius: 5px;
        }
        .link-press{
            text-decoration: none;
            color: white;
        }
        .option{
            width: 255px;
            text-decoration: none;
            background-color: rgb(186, 45, 45);
            color: white;
            border: none;
        }
        .role:hover{
            color: red;
            text-decoration: underline;
        }
    </style>
    
</head>
<body>
    <div class="container">
        <h1>Dashboard</h1><br>
        <p class="press"><a class="link-press" href="/product/new">Add New</a></p>
        <p class="press"><% if(category !== 'All'){ %>
            <a class="link-press" href="/product">Display All</a>
            <% } %></p><br />
        <div class="main">
            <% for (let product of products){  %>
                <div class="myCard"><a class="cardlink" href="/product/<%= product.id %>">
                    <div class="card" style="width: 18rem; height: 175px">
                        <div class="card-body">
                            <h5 class="card-title"><%= product.name %></h5>
                            <p>The Identification Number of <%= product.name %> is <%= product.identification %></p>
                            <p>The Role is: <a class="role" href="/product?category=<%= product.category %>"><%= product.category %></a></p>
                            <form action="/product/<%= product._id %>?_method=DELETE" method="post">
                            <button class="option">Delete</button>
                            </form>
                        </div>
                      </div>
                    </a>
                </div>
            <% } %>
        </div>
        
    </div>
    
</body>
</html>